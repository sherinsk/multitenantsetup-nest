version: '3.9'

services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: phpmyadmin
    restart: always
    environment:
      PMA_HOST: ${HOST}     
      PMA_PORT: ${PORT}            
      PMA_USER: ${USER}
      PMA_PASSWORD: ${PASSWORD}
    ports:
      - "8080:80"
